#include "Gomoku.hpp"
#include "Display.hpp"

int main(/*int argc, char **argv*/) {
	Display d;
	d.open();
	return(0);
}

// Revoir detection de double-three -> detecte quand le pion pose est entre les deux
// Sinon non